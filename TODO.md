# TODO - Failed Jobs functionality

- [ ] Add model/query helper for `failed_jobs` table usage (if needed)
- [ ] Add controller to display failed jobs in admin area
- [ ] Add blade view for failed jobs list
- [ ] Add routes (admin-only via existing role middleware)
- [ ] Add controller actions to (optionally) clear failed jobs safely
- [ ] Ensure code compiles (no syntax errors)
- [ ] Run `php artisan optimize:clear` and basic route check

